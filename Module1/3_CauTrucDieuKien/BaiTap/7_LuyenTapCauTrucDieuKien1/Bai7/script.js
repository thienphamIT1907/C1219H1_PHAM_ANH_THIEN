let time = parseInt(window.prompt('Nhập thời gian bạn đã gọi (phút): '));
let giaCuoc = 12000;
let pay = time * giaCuoc;

alert(`Phí cước điện thoại của bạn là ${pay}đ với giá cước ${giaCuoc}đ/phút !`);